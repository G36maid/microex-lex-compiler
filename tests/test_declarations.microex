Program TestDeclarations
Begin
    // Basic integer declarations
    declare i, count, index as integer;

    // Basic float declarations
    declare pi, e, phi as float;

    // Mixed declarations
    declare a, b[10], c as integer;
    declare x, y[20], z as float;

    // Array declarations
    declare matrix[100] as float;
    declare vector[50] as integer;

    // Multiple variables per line
    declare temp1, temp2, temp3, temp4 as float;
    declare idx1, idx2, idx3, idx4 as integer;
End
